const schedule = ["Bob", 123, true, 12.1];

schedule.push("Hey"); //push at the end
schedule.pop(); //removed last value

schedule.unshift("New one"); //add at the begginning
schedule.shift();//removed the first one

schedule.indexOf(123);//1

console.log(schedule);
